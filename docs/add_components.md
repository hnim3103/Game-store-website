# HƯỚNG DẪN SỬ DỤNG COMPONENTS (Ví dụ: Header)

---

## CÁCH CHÈN NHANH (QUICK START)

> Dành cho mọi thành viên khi muốn **chèn Header** vào trang HTML mới

Chỉ cần thêm dòng sau vào phần `<head>` của file HTML:

```html
<script src="/scripts/include-header.js" defer></script>
